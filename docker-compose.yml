services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_BACKEND_URL: ${NEXT_PUBLIC_BACKEND_URL:-https://eltiamcare.ir/api}
        BACKEND_URL: ${BACKEND_URL:-https://eltiamcare.ir/api}
        PULSE_API_URL: ${PULSE_API_URL}
        NEXT_PUBLIC_FIREBASE_API_KEY: ${NEXT_PUBLIC_FIREBASE_API_KEY}
        NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: ${NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN}
        NEXT_PUBLIC_FIREBASE_PROJECT_ID: ${NEXT_PUBLIC_FIREBASE_PROJECT_ID}
        NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: ${NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET}
        NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: ${NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID}
        NEXT_PUBLIC_FIREBASE_APP_ID: ${NEXT_PUBLIC_FIREBASE_APP_ID}
        NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID: ${NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID}
        NEXT_PUBLIC_FIREBASE_VAPID_KEY: ${NEXT_PUBLIC_FIREBASE_VAPID_KEY}
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8002}:8002"
    environment:
      NODE_ENV: production
      PORT: 8002
      HOSTNAME: 0.0.0.0
      # Cap V8 heap so a leak fails fast instead of eating the host
      NODE_OPTIONS: "--max-old-space-size=768"
      NEXT_PUBLIC_BACKEND_URL: ${NEXT_PUBLIC_BACKEND_URL:-https://eltiamcare.ir/api}
      BACKEND_URL: ${BACKEND_URL:-https://eltiamcare.ir/api}
      PULSE_API_URL: ${PULSE_API_URL}
      PULSE_API_KEY: ${PULSE_API_KEY}
    mem_limit: 1g
    cpus: "1.0"
